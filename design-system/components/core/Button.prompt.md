Uppercase tracked Montserrat button; use `gold` only for the main conversion CTA ("Request a Sample" / "Reserve Allocation"), `primary` elsewhere, `outline-dark` on forest grounds.

```jsx
<Button variant="gold">Request a Sample</Button>
<Button variant="outline-dark" size="sm">View the Standard</Button>
```
